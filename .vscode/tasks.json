{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test on docker-compose",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/tests"
            },
            "command": "docker-compose up --exit-code-from pytest; docker-compose down",
            "group": "test"
        }
    ]
}